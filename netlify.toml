# Netlify configuration for California Law (static site, no build step)
[build]
  publish = "."

# Optional: nicer URLs (e.g. /contact instead of /contact.html)
[[redirects]]
  from = "/contact"
  to = "/contact.html"
  status = 200
